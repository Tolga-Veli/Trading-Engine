#pragma once

namespace ob::Memory {

// Idea: create an array of memory pages
// have small, medium and large memory pages
// small -> Orders
// medium ->

class Pool {
public:
  Pool();

private:
};
} // namespace ob::Memory
