// TODO: the command ordering is not deterministic
// if multiple threads push a command to the command queue the one that comes
// first becomes the one that grabs the mutex first not the one that was first

class Sequencer {
public:
private:
};
